import * as chai from 'chai';

const expect = chai.expect;

describe('Question Unit Tests', () => {
    it ('Expect to run', () => {
        const check = true;
        expect(check).to.true;
    })
})

